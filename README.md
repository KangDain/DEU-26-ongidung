# backend&frontend 서버 여는 법
----------
Backend
```
cd backend  # 경로 이동
python -m venv venv  # 가상환경 새로 만들기
./venv/Scripts/pip install fastapi uvicorn sqlalchemy  # 필요한 라이브러리 설치 (Fast API 등)
./venv/Scripts/python -m uvicorn main:app --reload # 서버 실행
```

Frontend
```
cd frontend  # 경로 이동
flutter run -d chrome  # chrome으로 서버 실행
```

# 0506 수정
----------
DB구조 바껴있어서 다시 복구 (일단 기존 ERD대로 하고 나중에 추가 ex. 게임 등등)

app_state.dart 더미 데이터 삭제 and 진짜 데이터 드러나게 수정

약 복용 데이터 앱에서 등록 시 데이터에 등록 가능

app_service 부분 수정(back이랑 연동이 안되었기 때문)

admin으로 로그인 시 admin 페이지로 이동하게 수정

main.py에 보호자 피보호자 연동 코드 작성

피보호자, 보호자 둘이 서로에게 문자메세지 전송 시 서로의 알림창에 등록되도록 임시 수정 (메세지 기능 추가 시 DB 데이터 다 엎어야해서 일단 추후 논의)

+ 보호자  안정 tap에서 긴급이 뜨는 이유 → 임시로 DB에 피보호자 alert에 FALL_DOWN 등록함 (작동하는지 확인하기 위함)

# 0519 수정
----------
guardian이 recipient한테 메세지 전송 시 전송되지 않는 오류 수정
→ [전송]버튼을 눌러도 플러터에서 백엔드로 메세지를 보내지 않음

flutter 화면 깨짐 수정(overflow)

# 수정 부탁 사항
----------
회원 가입 > 1. 기본정보 > 보호자 연락처
→ 사용자 유형을 선택 후 독거노인OR아동일 때 보호자 연락처 입력 칸 작성하게 수정

회원 가입 > 2. 사용자 유형
→ 독거노인&아동 = recipient(피보호자) / 보호자 = guardian(보호자) / 일반 사용자 = ???
→ 어떻게 분류가 되었는지 설명 & 관리자는 회원가입이 아닌 데이터를 넣어 구성할것인지 등등

회원 가입 > 2. 사용자 유형:아동 클릭 시
→ 분류가 CHILD로 되어 기존 ADMIN/GUARDIAN/RECIPIENT로 나눴기에 회원가입이 안됌 (아동&어르신 RECIPIENT:피보호자로 묶어서 백으로 전송되게 수정 부탁)

관리자 페이지 > 로그아웃 기능 없음

관리자 페이지(admin_screen)와 보호자 페이지(guardian_screen), 피보호자 페이지(??)가 다 다른가? 
→ 정확한 설명이 없어 구분 어려움, 전체적인 페이지 정리해서 설명 부탁
→ 뭐가 어디서 쓰이는 dart 파일인지 구분이 어려움
→ ex. login 클릭 시 userType에 따라 어떤 screen에 연결 되어야하는지

지금 총 프론트가 다 구현된게 맞는지 확인해주세요.

# 질문사항&권유사항
----------

일단 코드가 꼬여있는 상태 이기에 admin, guardian, recipient는 home page 자체도 구분이 안가있는 상태. 경로 설정 문제인듯 함
→ 기본적인 경로 설정은 프론트에서 수정
→ 몇몇 제가 하려 했는데 제 코드가 아니라 복잡해서 부탁드려요

수정 시 이미 back과 front 합쳐놓은 상태이기에 수정한 레포지토리 pull해서 작업 후 main에 다시 push 
→ 수정 후 주석 제발 부탁드립니다. 어디 코드가 변경되었는지 일일히 하나하나 찾는건 시간 소모가 너무 커요..
→ 그래야 그 전 작업자의 수정을 기반으로 수정을 더할 수 있기때문, 안그러면 오히려 꼬임..
→ 혹시나 모르니 자신의 작업 환경 branch에 한번 더 올리는 것도 추천

DB아직 프론트랑 복잡하게 엮여서 테이블을 늘리기보단 일단 제대로 로그인, 회원가입 구현하고 테이블 더 늘리기
→ 테이블 늘렸으면 늘렸다고 말하기
→ 기본 ERD대로 하고, 추후에 추가.

실무직분들의 피드백도 좋지만 결국엔 저희가 하는 프로젝트입니다. 사소한거 하나하나 그냥 카톡에 물어보고 공유하고, 상세히 Readme 파일 작성해서 수정 한 부분, 부탁 사항 부분들 작성해서 읽고 파악하고 코드 작성해주세요.